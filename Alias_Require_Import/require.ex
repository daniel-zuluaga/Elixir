## Require

Integer.is_odd(3)
# sale un error por que para usar un macro se necesita utilizar el require

require(Integer)

# me dice que es un par RT/ true
Integer.is_even(4)
# me dice que es un impar RT/ true
Integer.is_odd(3)
