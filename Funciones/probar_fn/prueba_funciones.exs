IO.inspect(DefaultTest.do_work()) # va a obtener el parametro opcional de la funcion RT "hello"
IO.inspect(DefaultTest.do_work(true)) # obtiene el valor que le pasa como parametro RT/ true
IO.inspect(DefaultTest.do_work())
